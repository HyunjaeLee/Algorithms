---
data:
  _extendedDependsOn:
  - icon: ':heavy_check_mark:'
    path: graph/bipartite_coloring.hpp
    title: graph/bipartite_coloring.hpp
  _extendedRequiredBy: []
  _extendedVerifiedWith: []
  _isVerificationFailed: false
  _pathExtension: cpp
  _verificationStatusIcon: ':heavy_check_mark:'
  attributes:
    '*NOT_SPECIAL_COMMENTS*': ''
    PROBLEM: https://onlinejudge.u-aizu.ac.jp/problems/1404
    links:
    - https://onlinejudge.u-aizu.ac.jp/problems/1404
  bundledCode: "#line 1 \"test/graph/aoj1404.test.cpp\"\n#define PROBLEM \"https://onlinejudge.u-aizu.ac.jp/problems/1404\"\
    \n\n#line 1 \"graph/bipartite_coloring.hpp\"\n\n\n\n#include <optional>\n#include\
    \ <queue>\n#include <vector>\n\nstd::optional<std::vector<int>> bipartite_coloring(const\
    \ std::vector<std::vector<int>> &g) {\n    auto n = int(g.size());\n    std::vector<int>\
    \ color(n, -1);\n    for (auto i = 0; i < n; ++i) {\n        if (color[i] != -1)\
    \ {\n            continue;\n        }\n        std::queue<int> q;\n        q.push(i);\n\
    \        color[i] = 0;\n        while (!q.empty()) {\n            auto u = q.front();\n\
    \            q.pop();\n            for (auto v : g[u]) {\n                if (color[v]\
    \ == -1) {\n                    q.push(v);\n                    color[v] = color[u]\
    \ ^ 1;\n                } else if (color[v] == color[u]) {\n                 \
    \   return std::nullopt;\n                }\n            }\n        }\n    }\n\
    \    return color;\n}\n\n\n#line 4 \"test/graph/aoj1404.test.cpp\"\n#include <atcoder/modint>\n\
    #include <bits/stdc++.h>\nusing Z = atcoder::modint1000000007;\n\nint main() {\n\
    \    std::cin.tie(0)->sync_with_stdio(0);\n    int N, M;\n    std::cin >> N >>\
    \ M;\n    std::vector<int> A(N);\n    for (auto i = 0; i < N; ++i) {\n       \
    \ std::cin >> A[i];\n    }\n    std::vector<std::vector<int>> G(N);\n    for (auto\
    \ i = 0; i < N - 1; ++i) {\n        for (auto j = i + 1; j < N; ++j) {\n     \
    \       if (A[i] > A[j]) {\n                G[i].push_back(j);\n             \
    \   G[j].push_back(i);\n            }\n        }\n    }\n    auto result = bipartite_coloring(G);\n\
    \    if (!result) {\n        std::cout << \"0\\n\";\n        return 0;\n    }\n\
    \    auto color = std::move(*result);\n    std::vector<char> visited(N);\n   \
    \ std::vector<int> comp;\n    for (auto i = 0; i < N; ++i) {\n        if (visited[i])\
    \ {\n            continue;\n        }\n        comp.push_back(0);\n        auto\
    \ dfs = [&](auto &&self, int u) -> void {\n            visited[u] = true;\n  \
    \          comp.back() += (color[u] ? -1 : 1);\n            for (auto v : G[u])\
    \ {\n                if (!visited[v]) {\n                    self(self, v);\n\
    \                }\n            }\n        };\n        dfs(dfs, i);\n    }\n \
    \   auto C = int(comp.size());\n    std::vector<std::vector<Z>> dp(2, std::vector<Z>(2\
    \ * N + 1));\n    for (auto d = -N; d <= N; ++d) {\n        if (std::max(N + d,\
    \ N - d) <= 2 * M) {\n            dp[C & 1][N + d] = 1;\n        }\n    }\n  \
    \  for (auto i = C - 1; 0 <= i; --i) {\n        for (auto d = -N; d <= N; ++d)\
    \ {\n            dp[i & 1][N + d] = 0;\n            for (auto x : {d + comp[i],\
    \ d - comp[i]}) {\n                if (-N <= x && x <= N) {\n                \
    \    dp[i & 1][N + d] += dp[(i + 1) & 1][N + x];\n                }\n        \
    \    }\n        }\n    }\n    std::cout << dp[0][N].val() << \"\\n\";\n}\n"
  code: "#define PROBLEM \"https://onlinejudge.u-aizu.ac.jp/problems/1404\"\n\n#include\
    \ \"graph/bipartite_coloring.hpp\"\n#include <atcoder/modint>\n#include <bits/stdc++.h>\n\
    using Z = atcoder::modint1000000007;\n\nint main() {\n    std::cin.tie(0)->sync_with_stdio(0);\n\
    \    int N, M;\n    std::cin >> N >> M;\n    std::vector<int> A(N);\n    for (auto\
    \ i = 0; i < N; ++i) {\n        std::cin >> A[i];\n    }\n    std::vector<std::vector<int>>\
    \ G(N);\n    for (auto i = 0; i < N - 1; ++i) {\n        for (auto j = i + 1;\
    \ j < N; ++j) {\n            if (A[i] > A[j]) {\n                G[i].push_back(j);\n\
    \                G[j].push_back(i);\n            }\n        }\n    }\n    auto\
    \ result = bipartite_coloring(G);\n    if (!result) {\n        std::cout << \"\
    0\\n\";\n        return 0;\n    }\n    auto color = std::move(*result);\n    std::vector<char>\
    \ visited(N);\n    std::vector<int> comp;\n    for (auto i = 0; i < N; ++i) {\n\
    \        if (visited[i]) {\n            continue;\n        }\n        comp.push_back(0);\n\
    \        auto dfs = [&](auto &&self, int u) -> void {\n            visited[u]\
    \ = true;\n            comp.back() += (color[u] ? -1 : 1);\n            for (auto\
    \ v : G[u]) {\n                if (!visited[v]) {\n                    self(self,\
    \ v);\n                }\n            }\n        };\n        dfs(dfs, i);\n  \
    \  }\n    auto C = int(comp.size());\n    std::vector<std::vector<Z>> dp(2, std::vector<Z>(2\
    \ * N + 1));\n    for (auto d = -N; d <= N; ++d) {\n        if (std::max(N + d,\
    \ N - d) <= 2 * M) {\n            dp[C & 1][N + d] = 1;\n        }\n    }\n  \
    \  for (auto i = C - 1; 0 <= i; --i) {\n        for (auto d = -N; d <= N; ++d)\
    \ {\n            dp[i & 1][N + d] = 0;\n            for (auto x : {d + comp[i],\
    \ d - comp[i]}) {\n                if (-N <= x && x <= N) {\n                \
    \    dp[i & 1][N + d] += dp[(i + 1) & 1][N + x];\n                }\n        \
    \    }\n        }\n    }\n    std::cout << dp[0][N].val() << \"\\n\";\n}\n"
  dependsOn:
  - graph/bipartite_coloring.hpp
  isVerificationFile: true
  path: test/graph/aoj1404.test.cpp
  requiredBy: []
  timestamp: '2026-07-17 16:10:27+00:00'
  verificationStatus: TEST_ACCEPTED
  verifiedWith: []
documentation_of: test/graph/aoj1404.test.cpp
layout: document
redirect_from:
- /verify/test/graph/aoj1404.test.cpp
- /verify/test/graph/aoj1404.test.cpp.html
title: test/graph/aoj1404.test.cpp
---
